读通鉴工程执行规范（当前实际版）
================================

更新时间：2026-08-03
工作目录：/mnt/workspace/readgb
目标网站：https://www.dutongjian.com/

本文件是项目要求和当前实施边界。源码、测试和 PROJECT_STATE.md 优先于早期历史描述。PROJECT_STATE.md 同时承担任务清单、断点恢复记录和变更日志；DEVELOPMENT_LOG.md 已合并到它；每次开发必须先读 PROJECT_STATE.md。

一、交付目标
------------

- Kotlin 原生 Android App，禁止 WebView 套壳。
- Python/FastAPI 数据服务。
- 公开内容同步器、SQLite 索引、校验和 Android assets 导出。
- 可编译 Debug/Release APK。
- README、DOCS、站点分析、功能要求和可恢复任务记录。
- Backend 测试、Android JVM 测试、lint、APK 构建和 CI。
- Linux 主机上的爬虫 systemd 开机续跑。

当前实际限制：

- 新一轮全本同步尚未完成，不能宣称 APK 已包含 30,989 条全本正文。
- 正式 Release 签名必须由发布环境提供 keystore 和环境变量。
- 公开同步遵守 robots、同源、限速、重试和访问边界。

二、架构
--------

Android：

- data：Retrofit、Room、FTS、资产导入、TTS、AI 设置和持久化。
- domain：ReadingItem、目录、百科、Repository contract、字形映射和 TTS contract。
- ui：Compose、MVVM、StateFlow、详情阅读、学习图表、笔记和设置。
- di：Hilt 组装。

Backend：

- app/main.py：REST API。
- app/store.py：SQLite items、目录、百科和 HTTP cache。
- app/crawler.py：HTML robots-aware fetcher。
- app/parsers.py：无网络纯解析器。
- app/tongjian_sync.py：公开《资治通鉴》 JSON API 断点同步。
- app/export_android.py：正文、目录、百科资产校验和原子导出。
- tests：API、crawler、parser、sync 回归。

三、功能状态
------------

已完成并在源码或本地测试确认：

- Compose 原生 UI、MVVM、StateFlow、Repository、Room、Hilt。
- 离线优先启动、Room 收藏/历史/笔记、本地 FTS 搜索入口。
- 详情页原文/译文并读、单独阅读、字号、复制分享、划线笔记和工具面板。
- 展示层繁简/异体字映射和本地字词提示。
- 系统 TTS 控制器、句子状态、自动滚动和睡眠计时器代码。
- 学习页纪年/纪·朝代全量分组柱状图、阅读趋势、结构化人物全索引、中心人物共现关系图和文章下钻代码。
- FastAPI home/search/items/detail/目录/knowledge 接口。
- robots、缓存、Retry-After、退避、去重、checkpoint、受控 worker 和失败记录。
- 项目内 service/data 数据路径、清理前审计快照、断点恢复脚本和 systemd unit。
- CI 的 Python test/coverage 和 Android test/lint/build 任务。

未完成或必须实测：

- 完成 1,405 个纪年、校验 30,989 条正文。
- 生成并导入新的正文、目录和百科 assets。
- 全本数据下的图表覆盖、关系准确性和性能。
- AI 语法拆解、反事实推演、人物角色对话和战役沙盘。
- 自动 gh release 和 Codespace 签名发布闭环。

四、同步规范
------------

已确认公开接口：

- GET https://www.dutongjian.com/api/table_of_contents
- GET https://www.dutongjian.com/api/reign?reign_tongjian_id=<公开纪年 ID>

目标数据结构：

- 294 卷。
- 1,405 个纪年节点。
- 30,989 条正文。

默认命令：

    ./scripts/resume_crawler.sh

该命令只续跑，不带 --reset。数据全部放在项目目录：

    service/data/dutongjian.db
    service/data/tongjian-cache/
    service/data/tongjian-progress.json
    service/data/resync-archive-20260803/

新抓取内容不得放在 /tmp。同步器内部的 .tmp 仅是同目录原子写入中间文件。

宿主机持久化边界：只保证 `/mnt/workspace` 范围内的数据不会被定期清理；`/mnt/workspace` 之外的内容可能在实例运行一段时间后被清空。数据库、原始 JSON cache、checkpoint、锁和日志必须固定在 `/mnt/workspace/readgb/service/data/` 下。启动前先核对 `pwd` 和各数据路径的绝对位置，禁止使用 `/tmp`、宿主机其他目录或未确认的相对路径。

首次重新清理时才使用：

    PYTHONPATH=service python -m app.tongjian_sync \
      --allow-public-api \
      --reset \
      --database service/data/dutongjian.db \
      --cache-dir service/data/tongjian-cache \
      --checkpoint service/data/tongjian-progress.json \
      --expected-reigns 1405 \
      --expected-volumes 294 \
      --workers 4 \
      --min-interval 5.0 \
      --respect-robots

爬虫规则：

- robots.txt 无法读取时拒绝网络请求。
- 只访问配置 base URL 的公开路径。
- cache 命中不发请求。
- 请求启动受全局间隔控制，worker 数量有界。
- HTTP 429 读取 Retry-After，并共享冷却窗口。
- 每个纪年成功后更新 checkpoint。
- 单个纪年失败不丢失已完成结果；failed_reign_ids 和 last_errors 写入 checkpoint，下一次恢复重试。
- 不通过提高并发绕过站点限流。

五、恢复流程
------------

意外停止或机器重启后：

    cd /mnt/workspace/readgb
    sed -n '1,240p' PROJECT_STATE.md
    git status --short
    ps -eo pid,lstart,etime,pcpu,pmem,args | rg 'app.tongjian_sync|resume_crawler|readgb-crawler' | rg -v 'rg '
    jq '{total_reigns, completed: (.completed_reign_ids | length), failed: (.failed_reign_ids // [] | length), updated_at}' \
      service/data/tongjian-progress.json
    ./scripts/resume_crawler.sh

如果已有同步进程持有 service/data/tongjian-sync.lock，恢复脚本会退出，不启动重复进程。不要删除数据库、cache 或 checkpoint。只有确认需要从头清理时才使用 --reset。

开机服务：

    ./scripts/install_crawler_service.sh

systemd unit 使用 scripts/resume_crawler.sh，失败自动重启，不带 --reset。容器没有 systemd PID 1 时，安装脚本只负责留下 unit 和 enable 链接，宿主机启动后接管。

六、构建和测试
--------------

Backend：

    python3 -m pytest -q service/tests
    python3 -m compileall -q service/app

Android：

    cd android
    ./gradlew testDebugUnitTest
    ./gradlew lintDebug
    ./gradlew assembleDebug
    ./gradlew assembleRelease

CI：

.github/workflows/ci.yml 使用 Python 3.12、JDK 21、SDK 35，执行 Python 覆盖率测试和 Android test/lint/Debug/Release build，并缓存 pip/Gradle。CI 不运行真实网站同步。

七、提交和记录
--------------

每次开发必须：

1. 先读 PROJECT_STATE.md，再执行 git status --short；DEVELOPMENT_LOG.md 已与 PROJECT_STATE.md 合并，不维护第二份进度。
2. 在 PROJECT_STATE.md 当前任务清单写目标、路径和恢复方式。
3. 清单行使用 `- [ ]` 或 `- [x]`；完成项在同一行末追加 ✅，未完成项必须写下一步或阻塞原因。
4. 测试、同步、部署、数据路径和构建变化追加到同一文件的变更日志，不能只修改总结数字。
5. 变更日志使用 `### YYYY-MM-DD HH:MM Asia/Shanghai` 标题，并记录事项、命令、结果/实际数量、路径和恢复方式。
6. 只写已观察到的事实；阶段性 checkpoint、失败项和未实测的设备行为不得写成全量完成。
7. 提交前执行测试、git diff --check，并确认没有把数据库/cache/checkpoint/APK/keystore 加入 Git。
8. 用户要求推送时，提交后确认远端分支和 commit。

清单示例：

    - [ ] 目标：完成某项功能；路径：`文件路径`；恢复：`checkpoint 或命令`；下一步：说明阻塞。
    - [x] 目标：完成某项功能；结果：实际测试和数量；路径：`产物路径`。✅

日志示例：

    ### 2026-08-03 09:40 Asia/Shanghai
    - [x] 事项：恢复同步。命令：`./scripts/resume_crawler.sh`；结果：`82/1405`；路径：`service/data/tongjian-progress.json`；恢复：复用原 cache/checkpoint。✅

八、安全边界
------------

- 不登录、不绕过验证码、访问控制、robots 或付费墙。
- 不进行高频或攻击式抓取。
- 不泄露敏感信息。
- 不把原站会员能力描述为本项目能力。
- 不用未完成数据冒充全本。
