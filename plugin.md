# 资治通鉴研读 App 选定功能落地与设计方案

---

## 核心架构与离线底座 (P0)

* **全离线运行模式保障**
  * **本地数据库架构**：基于 SQLite (Room / FTS5) 构建本地核心存储，全量集成正文、胡三省注、地名字典及历代年表索引，检索与阅读过程零网络依赖。
  * **静态资源本地化打包**：静态知识库（字词字典、映射表、知识卡片）直接打包至应用 assets 或离线扩展包，首次启动建立本地增量索引，确保断网下毫秒级响应。

---

## 阅读器与音效体验 (P1)

* **TTS 定时关闭与当前朗读句高亮跟随**
  * **状态同步与自动滚动**：利用 Android `TextToSpeech` 的 `UtteranceProgressListener` 实时监听朗读进度，结合 Compose `LazyColumn` 的 `scrollToItem` 动态平滑滚动正文并应用动态高亮样式。
  * **睡眠倒计时**：基于 `CountDownTimer` 与后台协程服务，支持“15分钟 / 30分钟 / 播完本篇自动停止”的定时停播机制。

* **异体字与繁简通假字一键转换**
  * **无损视图映射**：集成古籍专用繁简及异体字映射表，仅在 UI 渲染层做文本转换，不改写底层数据库的原始史料文本。
  * **一键切换开关**：在阅读器控制栏提供“繁/简/异体字”实时切换入口。

---

## 史料可视化与专题索引 (P1)

* **典章制度与经济史专题索引**
  * **结构化多维打标**：对通鉴中的赋税（如均田制、两税法）、兵制（如府兵、募兵）、科举、货币（如开元通宝）等实体做分类打标。
  * **专题演变图谱**：提供制度演变的时间轴视图与相关史料切片列表。

* **门阀与世族树状谱系图**
  * **血缘关系树渲染**：使用 Canvas / 矢量图库构建魏晋至隋唐主要世家大族（如琅琊王氏、弘农杨氏）的血缘、联姻与官爵关系树。
  * **史料无缝联动**：点击谱系中的人物节点，可直接跳转至该人物在《通鉴》中的首次出场及相关章节。

* **历史事件热度与占比分析图表**
  * **篇幅与热度直方图**：基于数据可视化图表展示各朝代、帝王及年份在《通鉴》中的字数篇幅占比与事件发生密度。
  * **卷册下钻交互**：点击柱状图高亮区域可快速过滤并跳转至对应历史时期。

---

## 个人数据与桌面交互 (P2)

* **个人阅读统计看板**
  * **阅读行为埋点**：本地自动记录累计阅读时长、阅读字数及 294 卷的覆盖完成率。
  * **数据可视化大屏**：提供按周/月统计的阅读趋势图与研读偏好分析。

* **每日金句 / 历史上的今天**
  * **55**：采用 Android Glance / RemoteViews 框架开发。
  * **动态内容推送**：每日自动匹配展示《通鉴》名句、典故以及公历/干支历对应的“历史上的今天”。

---

## AI 辅助与高阶推演 (P2)

* **AI 智能辅助工具集**
  * **AI 历史角色对话模式**：注入当前章节上下文与历史人物性格 Prompt，提供基于史料事实的模拟问答。
  * **史实逻辑关联推演**：提供“假想推演”输入框，提取事件上下文输入大模型，分析反事实（Counterfactual）历史分支与可能性。
  * **古文语法拆解工具**：对选中长难句进行语法结构分析，使用不同颜色高亮主谓宾、倒装、介词结构与特殊句式。

* **重点事件“沙盘推演”模拟器**
  * **战役沙盘呈现**：针对重大战役（如官渡之战、赤壁之战、淝水之战），结合矢量地图展示双方兵力部署、粮道、地形胜负点与关键抉择走向。

---

## 工程构建与自动发布

* **Codespace 自动签名与 GitHub Release 发布**
  * **环境签名配置**：在 `app/build.gradle` 中配置从环境变量（如 `KEYSTORE_FILE`、`KEY_ALIAS`、`STORE_PASSWORD`）读取签名密钥。
  * **Codespace 终端构建**：在 GitHub Codespace 中直接运行 `./gradlew assembleRelease` 触发编译与打包签名。
  * **gh CLI 一键 Release**：不依赖 GitHub Actions 工作流，直接在 Codespace 终端通过 `gh release create vX.Y.Z app-release.apk` 命令行一键生成 Release 并上传 APK 产物。
